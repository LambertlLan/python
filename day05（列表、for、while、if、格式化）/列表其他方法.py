# __author: Lambert
# __date: 2017/8/16 16:11
a = [1, 2, 3, 1, 4, 5]
b = ['a', 'c', 'b']
# print(a.count(1))  # 计算1在a中出现的次数
# a.extend(b)  # 把B数组中的值合并到a数组，类似于js concat()
# print(a)
# print(b.index('a'))  # 类似于indexOf()
# a.reverse()  # 调换顺序
# b.sort()  # 排序，数字从大到小，英文字母顺序
print(6 in a)  # 检测6在不在a中
